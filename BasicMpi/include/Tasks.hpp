#ifndef SCIENTIFICCOMPUTING_TASKS_HPP
#define SCIENTIFICCOMPUTING_TASKS_HPP

#include <cstddef>

void Demo();
void CheckNoPrimes(std::size_t maxNumber);
void CheckNoPrimesPlain(std::size_t maxNumber);
void CheckNoPrimesMsgs(std::size_t maxNumber);

#endif // SCIENTIFICCOMPUTING_TASKS_HPP
